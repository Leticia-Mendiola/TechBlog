<section class="card">
  <a href="/post/{{id}}">
    <header>{{post-title}}}</header>
    <div>
      <h1>{{username}}</h1><h3>{{created_at}}</h3><h3>{{updated_at}}</h3>
    </div>
    <p>
      {{post_body}}
    </p>
  </a>
</section>